/*
Assignment statement
    An instruction that stores a value in a variable

In C the symbol = is the assignment operator



*/

int main()
{
    int age = 21;
    double income = 5030.89;
    char initial = 'B';

    // The "=" assigns a value to the variable
    // We can think of it as "takes the value of", "becomes", or "gets"
    
    return 0;
}
