/**
 * Preprocessor - 
 * – A program that modifies a C program prior to its
 * compilation by text substitution. All preprocessor commands begin with a hash
 * symbol (#).
 **/

#include <stdio.h>
#define PI 3.14159

int main()
{
    //What's PI?
    printf("This is PI: %f", PI);
    return 0;
}
